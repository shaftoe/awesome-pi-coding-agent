/**
 * Static site generator — produce a searchable HTML directory.
 */
/** Generate the static HTML site. */
export function generateSite(_entries: unknown, _outputDir: string): void {
	throw new Error("Not yet implemented — Phase 4");
}
